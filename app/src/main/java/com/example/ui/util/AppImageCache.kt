package com.example.ui.util

import android.content.Context
import androidx.compose.ui.graphics.ImageBitmap

/**
 * Thread-safe in-memory cache for app images.
 */
object AppImageCache {
  fun getSandwichBitmap(context: Context): ImageBitmap? = null
}
