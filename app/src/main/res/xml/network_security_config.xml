<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base config: Require HTTPS by default for all connections (Play Store Security Compliant) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Permit cleartext HTTP strictly for local development IPs and Android emulator -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.11.16</domain>
        <domain includeSubdomains="true">192.168.17.21</domain>
    </domain-config>
</network-security-config>
